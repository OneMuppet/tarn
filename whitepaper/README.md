# tarn whitepaper

`tarn-whitepaper.html` — the source (self-contained; pulls the mascot from
`../assets/mascot.svg`). `tarn-whitepaper.pdf` — the rendered 7-page PDF.

Re-render the PDF (needs Chrome):

```sh
./render.sh
```

Engineering field-notes style adapted to tarn's copper palette; every figure is
a real measurement (see the repo's Performance section and `docs/INDEX.md`).
