export const fetchData = async (
    url,
) => {
    return url;
};

const add = (a, b) => a + b;

export enum Color {
    Red,
    Blue,
}

function useThing() {
    const local = () => fetchData("x");
    return local;
}
